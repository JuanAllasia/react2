import React from "react";

const CartWidget = () => {
    return(
        <div className="cart-widget">
            <p className="carritologo">carrito logo</p>
        </div>
    );
};

export default CartWidget;